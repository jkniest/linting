#!/usr/bin/env bash

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )"

echo ""
echo ""

echo "Installing PHP CS fixer configuration"
cp vendor/jkniest/linting/stubs/.php_cs .

echo ""
echo ""
